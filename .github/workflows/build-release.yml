name: Build & Release Native Flutter APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build & Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Create local.properties
        run: |
          mkdir -p VaultX/android
          echo "flutter.sdk=$FLUTTER_ROOT" > VaultX/android/local.properties
        shell: bash

      - name: Install Dependencies
        working-directory: VaultX
        run: flutter pub get

      - name: Build Release APK
        working-directory: VaultX
        run: flutter build apk --release --no-tree-shake-icons

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          # This wildcard ensures it finds the APK even if paths differ slightly
          files: "VaultX/build/app/outputs/flutter-apk/app-release.apk"
          tag_name: "v1.2.${{ github.run_number }}"
          name: "VaultX Pro Update (Build ${{ github.run_number }})"
          body: |
            🚀 **VaultX Pro Native Build Successful**
            
            **Changes:**
            - Premium UI/UX Overhaul (Linear Style)
            - Fixed QR Code Snap Sharing
            - Added Developer Contacts (Telegram, Email, GitHub)
            - Improved Security Keypad
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN_CLASSIC }}